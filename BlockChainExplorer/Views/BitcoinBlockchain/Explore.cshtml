@model BlockChainExplorer.Models.BitcoinBlockchain


@using (Html.BeginForm("Block", "AnalyzeBlock", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Analyze a block</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        <table width="100%">
            <tr>
                <td name="blocknumber_row">
                    @Html.LabelFor(m => m.blocknumber, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => m.blocknumber, new { @class = "form-control" })
                    </div>
                </td>
            </tr>
        </table>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Submit" />
        </div>
    </div>
}

@using (Html.BeginForm("Transaction", "AnalyzeTransaction", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Analyze a transaction</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        <table width="100%">
            <tr>
                <td name="transactionid_row">
                    @Html.LabelFor(m => m.transactionid, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => m.transactionid, new { @class = "form-control" })
                    </div>
                </td>
            </tr>
         </table>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Submit" />
        </div>
    </div>
}

@using (Html.BeginForm("Wallet", "AnalyzeWallet", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Analyze a wallet</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        <table width="100%">
            <tr>
                <td name="walletid_row">
                    @Html.LabelFor(m => m.walletid, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => m.walletid, new { @class = "form-control" })
                    </div>
                </td>
            </tr>
        </table>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Submit" />
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to Blockchain Explorer", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
